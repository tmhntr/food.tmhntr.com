import React from 'react'

// this page will have all the recipe cards listed and a search / filter at the top
// users saved recipes will show up at the top if they show up if selected
const recipes = () => {
  return (
    <div>recipes</div>
  )
}

export default recipes